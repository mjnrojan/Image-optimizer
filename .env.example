# Directory containing your images/videos (relative or absolute)
ASSETS_DIR=./src/assets

# WebP settings
WEBP_QUALITY=80        # 0-100 (higher = better quality, larger file)
WEBP_EFFORT=6          # 0-6   (higher = slower, better compression)

# AVIF settings
AVIF_QUALITY=75        # 0-100 (higher = better quality, larger file)
AVIF_EFFORT=6          # 0-9   (higher = slower, better compression)

# Lossless mode (true/false)
LOSSLESS=false
